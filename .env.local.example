# 复制本文件为 .env.local（Next.js 会在 dev/build 时加载）。
# Hooks 脚本（~/.cursor/scripts/*.mjs）不读取此文件，需在 shell 中 export 相同变量，
# 或使用与下方一致的绝对路径，否则 Web 与采集可能指向不同文件。
#
# 默认路径（未设置任何变量时）：
#   ~/cursor-events.jsonl      事件
#   ~/thinking-corpus.jsonl    Thinking 语料
#   ~/prompt-corpus.jsonl      用户提问
#
# 变量别名与优先级见 README「路径变量总览」与 CORPUS↔THINKING、EVENTS↔CURSOR 对照表。

# --- 事件（优先 EVENTS_JSONL_PATH，其次 CURSOR_EVENTS_PATH）---
# EVENTS_JSONL_PATH=/absolute/path/cursor-events.jsonl
# CURSOR_EVENTS_PATH=/absolute/path/cursor-events.jsonl

# --- Thinking 语料（优先 CORPUS_JSONL_PATH，其次 THINKING_CORPUS_PATH）---
# CORPUS_JSONL_PATH=/absolute/path/thinking-corpus.jsonl
# THINKING_CORPUS_PATH=/absolute/path/thinking-corpus.jsonl

# --- 用户提问（无别名）---
# PROMPT_CORPUS_PATH=/absolute/path/prompt-corpus.jsonl

# --- 可选关键词页（仅 Web；未配置时 /api/keyword 返回 503）---
# KEYWORD_JSONL_PATH=/absolute/path/keyword.jsonl

# --- 按日分片与保留（Hooks 与 Web 共用；默认：按日写入、保留 90 天）---
# JSONL_DAILY_SPLIT=0
# JSONL_RETENTION_DAYS=90
# MAX_JSONL_BYTES=52428800
# MAX_JSONL_TAIL_LINES=100000
