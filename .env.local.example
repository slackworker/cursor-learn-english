# 复制本文件为 .env.local（Next.js 会在 dev/build 时加载）。
# Hooks 脚本（~/.cursor/scripts/*.mjs）不直接读取本文件；请用安装器写入
# ~/.cursor/cursor-learn-english.paths.json，或在 Hooks 运行环境 export 相同变量。
#
# 推荐：先跑安装器（会写 paths.json，并默认把 dataDir 指到本仓库 data/）
#   node scripts/setup-cursor-hooks.mjs
#   # WSL + 还有 Windows 项目时：
#   node scripts/setup-cursor-hooks.mjs --also-windows
# 然后打开仪表盘 /setup 做体检。
#
# 默认路径（未设置任何变量且无 paths.json 时）：
#   ~/projects/cursor-learn-english/data/cursor-events.jsonl
#   ~/projects/cursor-learn-english/data/thinking-corpus.jsonl
#   ~/projects/cursor-learn-english/data/prompt-corpus.jsonl
#
# 变量别名与优先级见 README「路径变量总览」与 CORPUS↔THINKING、EVENTS↔CURSOR 对照表。

# --- 数据目录（可选；覆盖三项默认基路径所在目录）---
# 优先级：CURSOR_DASHBOARD_DATA_DIR > ~/.cursor/cursor-learn-english.paths.json > 默认路径
# CURSOR_DASHBOARD_DATA_DIR=/absolute/path/to/data

# --- 事件（优先 EVENTS_JSONL_PATH，其次 CURSOR_EVENTS_PATH）---
# EVENTS_JSONL_PATH=/absolute/path/cursor-events.jsonl
# CURSOR_EVENTS_PATH=/absolute/path/cursor-events.jsonl

# --- Thinking 语料（优先 CORPUS_JSONL_PATH，其次 THINKING_CORPUS_PATH）---
# CORPUS_JSONL_PATH=/absolute/path/thinking-corpus.jsonl
# THINKING_CORPUS_PATH=/absolute/path/thinking-corpus.jsonl

# --- 用户提问（无别名）---
# PROMPT_CORPUS_PATH=/absolute/path/prompt-corpus.jsonl

# --- Cursor 侧边栏会话标题（优先 state.vscdb composerHeaders.name）---
# 未设置时会自动探测 Windows AppData / WSL /mnt/c/Users/*/AppData/...
# CURSOR_STATE_VSCDB=/mnt/c/Users/You/AppData/Roaming/Cursor/User/globalStorage/state.vscdb
# STATE_VSCDB_PATH=/absolute/path/state.vscdb
# 次选：conversation-search.db（索引可能滞后）
# CURSOR_CONVERSATION_SEARCH_DB=/mnt/c/Users/You/AppData/Roaming/Cursor/User/globalStorage/conversation-search.db
# CONVERSATION_SEARCH_DB_PATH=/absolute/path/conversation-search.db

# --- 会话标题来源（优先 AGENT_TRANSCRIPTS_PATH，其次 CURSOR_AGENT_TRANSCRIPTS_PATH）---
# AGENT_TRANSCRIPTS_PATH=/absolute/path/agent-transcripts
# CURSOR_AGENT_TRANSCRIPTS_PATH=/absolute/path/agent-transcripts

# --- 可选关键词页（仅 Web；未配置时 /api/keyword 返回 503）---
# KEYWORD_JSONL_PATH=/absolute/path/keyword.jsonl

# --- 按日分片与保留（Hooks 与 Web 共用；默认：按日写入、不自动删分片）---
# JSONL_DAILY_SPLIT=0
# JSONL_RETENTION_DAYS=0
# 若要恢复自动删过期分片，设为天数，例如：JSONL_RETENTION_DAYS=90
# MAX_JSONL_BYTES=52428800
# MAX_JSONL_TAIL_LINES=100000
