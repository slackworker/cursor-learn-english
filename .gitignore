node_modules
.next

# Next.js 自动生成（dev/build 会改写，勿提交）
next-env.d.ts

# 本地临时文档与笔记（不入库）
.local/

# 环境变量文件
.env
.env.local
.env*.local
.env.production
.env.production.local

# Hooks / 仪表盘采集数据（本地 jsonl，不入库）
/data/*.jsonl
/data/.*-jsonl-prune

# orphan-cleanup 脚本备份（体积大，仅本地保留）
/data/.orphan-cleanup-backup-*/
