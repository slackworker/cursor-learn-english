node_modules
.next

# TypeScript incremental build cache（本地生成，勿提交）
*.tsbuildinfo

# Next.js 自动生成（dev/build 会改写，勿提交）
next-env.d.ts

# 本地临时文档与笔记（不入库）
.local/

# 环境变量文件
.env
.env.local
.env*.local
.env.production
.env.production.local

# Hooks / 仪表盘采集数据（本地 jsonl，不入库）
/data/*.jsonl
/data/.*-jsonl-prune

# 学习者 Pass 词汇进度（本机 / 局域网共享，不入库）
/data/vocab-passed.json

# 词典构建缓存（原始下载，体积大；生成结果 *.generated.json 可入库）
/data/.dict-cache/

# orphan-cleanup 脚本备份（体积大，仅本地保留）
/data/.orphan-cleanup-backup-*/
